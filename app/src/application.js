define(['utils/math', 'ui/button', 'ui/input'],
        function(math, button, input) {
            // normally ui deps go to elsewhere.
            // just illustrating the point here
    return {
        run: function() {
            // get the app running now
            // ...
        }
    };
});
